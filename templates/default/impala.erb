# generated by chef, changes will be overwritten

<% @options.each do |key, value| %>
  export <%= key.upcase %>="<%= value %>"
<% end %>
  export IMPALA_SERVER_ARGS="-log_dir=${IMPALA_LOG_DIR} -catalog_service_host=${IMPALA_CATALOG_HOST} -catalog_service_port=${IMPALA_CATALOG_PORT} -use_statestore -state_store_host=${IMPALA_STATE_STORE_HOST} -state_store_port=${IMPALA_STATE_STORE_PORT} -be_port=${IMPALA_BACKEND_PORT}"
  export IMPALA_STATE_STORE_ARGS=" -log_dir=${IMPALA_LOG_DIR} -state_store_port=${IMPALA_STATE_STORE_PORT}"
  export IMPALA_CATALOG_ARGS=" -log_dir=${IMPALA_LOG_DIR} "